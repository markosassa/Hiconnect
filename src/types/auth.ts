export type User = {
  id: number;
  email: string;
  codsoc: number;
  nome: string;
};

export type Role = {
  idrole: number;
  slug: string;
  namerole: string;
};

export type Funzione = {
  idfunzione: number;
  slug: string;
};